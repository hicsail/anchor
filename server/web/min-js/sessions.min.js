function deleteDoc(e){
  const s = $('#' + e); s.text() === 'Delete' ? s.text('Are You Sure?') : $.ajax({ url:'../api/sessions/' + e,type:'DELETE',success:function (e){
    successAlert('Session Deleted'),table.ajax.reload();
  },error:function (e){
    errorAlert(e.responseJSON.message);
  } });
}

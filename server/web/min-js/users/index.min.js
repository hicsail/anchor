function deleteDoc(e){
  const r = $('#' + e); r.text() === 'Delete' ? r.text('Are You Sure?') : $.ajax({ url:'../api/users/' + e,type:'DELETE',success:function (e){
    successAlert('User Deleted'),table.ajax.reload();
  },error:function (e){
    errorAlert(e.responseJSON.message);
  } });
}
